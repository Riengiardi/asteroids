This is my file that I read before I start programming during a day

Tips to self:

* Think about doing something to UI as in make it inherit Sprite and use Group logic as other objects or create
another approach that will fill more bloaty as new elements are added
* Think about adding some flashing text after game over which will ask you to click "ENTER" to exit the game or play again
* Think about adding particle effect for the space jet engine

Done:

Add a scoring system
Created Text object usable in multiple cases
Implement multiple lives and respawning
Add an explosion effect for the asteroids
Add acceleration to the player movement
Make the player wrap around the screen instead of disappearing (I don't feel like asteroids should wrap around)

Before proceeding with:

Create different weapon types
Make the asteroids lumpy instead of perfectly round
Make the ship have a triangular hit box instead of a circular one
Add a shield power-up
Add a speed power-up
Add bombs that can be dropped
